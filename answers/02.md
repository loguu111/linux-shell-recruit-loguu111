1
前者有输入./，系统就会在给定的路径前往寻找。后者没输入，系统就会在默认的PATH环境变量内寻找，PATH系统默认设置为/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin:/snap/bin，该目录下没有recruit-info因此找不到。
用export中对PATH输入错误的路径也会被系统添加
2
Shell 会在 PATH 环境变量包含的目录中依次查找 ls。
通常 ls 位于 PATH路径中，因此可以直接运行。
